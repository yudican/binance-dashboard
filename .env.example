# Optional: single-user/server-side-only mode.
# If both are set, the API route will use these and ignore credentials from the browser.
# BINANCE_API_KEY=your_api_key
# BINANCE_API_SECRET=your_api_secret
